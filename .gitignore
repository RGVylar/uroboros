/.claude
/handoff
backend/backend.err
backend/backend.log
backend/backend.log
*.keystore
/.claude
/.claude
/.claude
mockup-changelog.html
mockup-mood.html
.claude/launch.json
.claude/settings.local.json
QA_TEST_ACCOUNT.md

# Documentos de trabajo local: suelen llevar datos personales (nombres reales,
# cuentas de prueba). El repo es PUBLICO, nunca deben subirse.
mockup-*.html
PLAN_*.md
QA_REVIEW_*.md

# Boveda de Obsidian con la documentacion tecnica: menciona dominios, IDs de
# contenedor, cuentas de prueba y comandos de produccion.
/documentation/

# Tickets de compra del spike de OCR (PLAN_ticket_despensa, paso 0) y el volcado
# crudo de Tesseract. Un ticket lleva comercio, fecha, hora y a veces los ultimos
# digitos de la tarjeta. El repo es PUBLICO: ni las fotos ni el texto salen de aqui.
backend/scripts/receipt_samples/
backend/scripts/receipt_spike_out/
